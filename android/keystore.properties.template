# Android Keystore配置模板
# 在构建过程中，此文件将被实际的keystore.properties替换

# Keystore文件路径（相对于android目录）
storeFile=keystore.jks

# Keystore密码
storePassword=${KEYSTORE_PASSWORD}

# 密钥别名
keyAlias=${KEY_ALIAS}

# 密钥密码
keyPassword=${KEY_PASSWORD}