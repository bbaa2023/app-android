// WebView应用配置文件
// 注意：此文件仅用于参考，实际配置应从.env文件加载
// Tauri应用的主要配置在src-tauri/tauri.conf.json中

module.exports = {
  // 此配置文件已被弃用，所有配置现在从.env文件加载
  // 请参考.env.local文件获取当前配置
  
  // 默认加载的网页URL - 从.env文件中的DEFAULT_URL或REMOTE_URL加载
  // 窗口配置 - 从.env文件中的WINDOW_*变量加载
  // 应用功能配置 - 从.env文件中的ENABLE_*变量加载
  
  // 此文件保留仅用于向后兼容，未来版本将完全移除
};